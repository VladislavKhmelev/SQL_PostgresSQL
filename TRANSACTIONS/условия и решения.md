~~~

Клиент 105 хочет купить 2 единицы товара id=2.

Оформи весь процесс одной транзакцией:

Проверить существование товара.
Проверить доступный остаток.
Зарезервировать 2 единицы.
Создать заказ.
Рассчитать стоимость заказа.
Создать соответствующую запись в account_operations.
Сохранить всё через COMMIT.
Если любой этап не выполнен — выполнить ROLLBACK.

После выполнения покажи только необходимые поля товара, заказа и операции.

BEGIN;

SELECT
*
from products
where id = 2 and stock - reserved >= 2;

UPDATE products
SET reserved = reserved + 2
where id = 2 and stock - reserved >= 2;

insert into orders_tx
(customer_id, product_id, quantity, status, created_at)

VALUES
(105, 2, 2, 'new', CURRENT_DATE);

INSERT into account_operations
(account_id, operation_type, amount, created_at, status)

VALUES
(2,'withdrawal', (SELECT
                    price * 2
                    from products
                    where id = 2) , CURRENT_DATE, 'completed');


ROLLBACK;


SELECT
products.id,
name,
price,
quantity,
operation_type,
created_at

from products
INNER JOIN orders_tx
on orders_tx.product_id = products.id 
INNER JOIN accounts
on accounts.id = orders_tx.customer_id
INNER JOIN account_operations
on account_operations.account_id = accounts.id;


| id | name  | price | quantity | operation_type | created_at |
| -: | ----- | ----: | -------: | -------------- | ---------- |
|  2 | Phone | 50000 |        2 | withdrawal     | 2026-09-02 |


Что произошло с products

| id | name  | price | stock | reserved |
| -: | ----- | ----: | ----: | -------: |
|  2 | Phone | 50000 |    20 |        5 |


После UPDATE:
| id | name  | price | stock | reserved |
| -: | ----- | ----: | ----: | -------: |
|  2 | Phone | 50000 |    20 |    **7** |

Новый заказ
| customer_id | product_id | quantity | status |
| ----------: | ---------: | -------: | ------ |
|         105 |          2 |        2 | new    |

Новая операция
| account_id | operation_type | amount | status    |
| ---------: | -------------- | -----: | --------- |
|          2 | withdrawal     | 100000 | completed |





Сделай перевод 7000:

id=3 → id=2

В транзакции должны быть:

проверка баланса;
списание;
зачисление;
withdrawal;
deposit.

После выполнения одной из операций специально вызови ошибку и сделай ROLLBACK.

Покажи, что произошло со всеми изменениями.

BEGIN;

SELECT
*
from accounts
where (id = 2 and status = 'active') or (id = 3 and balance >= 7000 and status = 'active');


UPDATE accounts
SET balance = balance - 7000
where id = 3 and balance>= 7000 and status = 'active';


UPDATE accounts
SET balance = balance + 7000
where id = 2 and status = 'active';

INSERT into account_operations
 (account_id, operation_type, amount, created_at, status)

VALUES
(3, 'withdrawal', 7000, CURRENT_DATE, 'completed');


INSERT into account_operations
 (account_id, operation_type, amount, created_at, status)

VALUES
(2, 'deposit', 7000, CURRENT_DATE, 'completed');

ROLLBACK;




В одной транзакции:

Увеличь баланс id=1 на 5000.
Создай deposit.
Получи текущую информацию по счету:
owner;
balance;
количество операций;
общую сумму завершённых операций.
Затем выполни ROLLBACK.

Покажи, что было видно внутри транзакции и что осталось после отката.

BEGIN;

UPDATE accounts
SET balance = balance + 5000
where id = 1;


insert into account_operations
(account_id, operation_type, amount, created_at, status)

VALUES
(1,'deposit', 5000, CURRENT_DATE, 'completed');


with tt as (
SELECT
id,
owner,
balance,

count(account_operations.id) over(PARTITION BY account_id) as r,

sum(case when status = 'completed' then amount end ) over(PARTITION BY account_id) as r1,

ROW_NUMBER() over(PARTITION BY account_id) as r2

from accounts
INNER JOIN account_operations
on account_operations.account_id = accounts.id
where id = 1
)

select
*
from tt 
where r2 = 1;


ROLLBACK;

| id | owner | balance |  r |    r1 | r2 |
| -: | ----- | ------: | -: | ----: | -: |
|  1 | Alex  |   20000 |  4 | 16000 |  1 |

После ROLLBACK
| id | owner | balance |
| -: | ----- | ------: |
|  1 | Alex  |   15000 |





Представь, что для товара осталось:

stock = 5
reserved = 3

Два клиента одновременно пытаются купить по 2 единицы.

Смоделируй транзакции так, чтобы:

оба клиента не смогли одновременно «забрать» одни и те же единицы;
один клиент успешно получил резерв;
второй получил отказ;
не возникло отрицательного доступного остатка.


BEGIN;


select *
from products
where id = 3 and stock - reserved >= 2
for UPDATE;

UPDATE products
SET reserved = reserved + 2
where id = 3 and stock - reserved >= 2;


commit;


BEGIN;


select *
from products
where id = 3 and stock - reserved >= 2
for UPDATE;

ROLLBACK;

| stock | reserved | available |
| ----: | -------: | --------: |
|     5 |        5 |         0 |

Поэтому SELECT не вернёт строку, и второй клиент понимает: резервировать нечего → ROLLBACK.








Начни оформление заказа для клиента 103 на 5 единиц товара id=5.

В процессе транзакции:

Зарезервируй товар.
Создай заказ.
Создай операцию, связанную с покупкой.
Представь, что на последнем этапе произошла ошибка.
Выполни ROLLBACK.

BEGIN;


UPDATE products
SET reserved = reserved + 5
where id = 5 and stock - reserved >= 5;

insert into orders_tx
(customer_id, product_id, quantity, status, created_at)


VALUES

(103,5,5,'paid',CURRENT_DATE);

insert into account_operations
(account_id, operation_type, amount, created_at, status)

VALUES
(103, 'withdrawal', 5*3000, CURRENT_DATE, 'completed');

ROLLBACK;






Нужно атомарно:

проверить доступный остаток;
изменить stock;
изменить reserved;
создать заказ;
если проверка не пройдена — ничего не сохранять.

BEGIN;

select *
from products
where id = 1 and stock - reserved >= 3;


UPDATE products
SET reserved = reserved + 3
where id = 1 and stock - reserved >= 3;

INSERT into orders_tx
(customer_id, product_id, quantity, status, created_at)


VALUES

(102, 1, 3, 'new', CURRENT_DATE);

COMMIT;








Реализуй перевод 6000 со счёта id=3 на id=1.

В одной транзакции необходимо:

проверить существование обоих счетов;
проверить их статус;
проверить достаточность средств;
выполнить списание;
выполнить зачисление;
создать две записи в account_operations;
при любой проблеме изменения не должны сохраниться.

BEGIN;

SELECT
*
from accounts
where ( id = 1 and status = 'active') 
or (id = 3 and status = 'active' and balance >= 6000);


UPDATE accounts
SET balance = balance - 6000
where id = 3;

UPDATE accounts
SET balance = balance + 6000
where id = 1;


insert into account_operations
(account_id, operation_type, amount, created_at, status)


VALUES

(3, 'withdrawal', 6000, CURRENT_DATE, 'completed');


insert into account_operations
(account_id, operation_type, amount, created_at, status)


VALUES

(1, 'deposit', 6000, CURRENT_DATE, 'completed');


COMMIT;



Для товара id=5 одновременно:

увеличить stock на 30;
уменьшить reserved на 10;
увеличить цену на 500.

BEGIN;

UPDATE products
SET reserved = reserved - 10, stock = stock + 30, price = price + 500
where id = 5;

COMMIT;

| Поле       |   До | После COMMIT |
| ---------- | ---: | -----------: |
| `stock`    |  100 |      **130** |
| `reserved` |   20 |       **10** |
| `price`    | 3000 |     **3500** |




В одной транзакции:

Списать 4000 со счёта id=1.
Создать withdrawal на 4000.
Зачислить 4000 на счёт id=3.
Создать deposit на 4000.
Выполнить ROLLBACK.

Проверь, что ни одно из четырёх изменений не осталось.

BEGIN;

UPDATE accounts
SET balance = balance - 4000
where id = 1 and balance >= 4000;

insert into account_operations
(account_id, operation_type, amount, created_at, status)

VALUES

(1, 'withdrawal', 4000, CURRENT_DATE, 'completed');

UPDATE accounts
SET balance = balance + 4000
where id = 3;

insert into account_operations
(account_id, operation_type, amount, created_at, status)

VALUES

(3, 'deposit', 4000, CURRENT_DATE, 'completed');

ROLLBACK;

| Этап              | Balance id=1 | Balance id=3 | Withdrawal  | Deposit     |
| ----------------- | -----------: | -----------: | ----------- | ----------- |
| До                |        15000 |        25000 | —           | —           |
| Внутри транзакции |        11000 |        29000 | создан      | создан      |
| После ROLLBACK    |    **15000** |    **25000** | отсутствует | отсутствует |




Клиент 101 покупает 2 единицы товара id=4.

В одной транзакции:

проверить наличие товара;
уменьшить stock;
увеличить reserved;
создать заказ;
всё сохранить через COMMIT.

BEGIN;

UPDATE products
SET reserved = reserved + 2
where id = 4 and stock - reserved >= 2;


insert into orders_tx
(customer_id, product_id, quantity, status, created_at)

VALUES

(101, 4, 2, 'new', CURRENT_DATE);

COMMIT;

| Поле                        | До | После COMMIT |
| --------------------------- | -: | -----------: |
| `stock`                     | 50 |       **50** |
| `reserved`                  | 10 |       **12** |
| Доступно `stock - reserved` | 40 |       **38** |




Отмени orders_tx.id=8.

В одной транзакции:

статус заказа → cancelled;
вернуть количество товара в stock;
уменьшить reserved;
сохранить все изменения через COMMIT.

BEGIN;

UPDATE orders_tx
SET status = 'cancelled'
where id = 8;

UPDATE products
SET reserved = reserved - 4
where id = 5;


COMMIT;

| Поле                     |     До | Внутри транзакции |  После COMMIT |
| ------------------------ | -----: | ----------------: | ------------: |
| `orders_tx.id=8` status  | `paid` |       `cancelled` | **cancelled** |
| `products.id=5` reserved |     20 |                16 |        **16** |
| `products.id=5` stock    |    100 |               100 |       **100** |





В одной транзакции:

Увеличь баланс счёта id=1 на 2000.
Создай deposit на 2000.
Измени статус счёта на blocked.
Выполни откат.

Покажи состояние счёта и операции после ROLLBACK.

BEGIN;

UPDATE accounts
SET balance = balance + 2000
where id = 1;


insert into account_operations
(account_id, operation_type, amount, created_at, status)

VALUES
(1, 'deposit', 2000, CURRENT_DATE, 'completed');

UPDATE accounts
SET status = 'blocked'
where id = 1;


ROLLBACK;




Для товара id=2 создай заказ на 4 единицы.

Нужно:

проверить доступное количество stock - reserved;
зарезервировать 4 единицы;
создать заказ;
сохранить изменения одной транзакцией.

BEGIN;

UPDATE products
SET reserved = reserved + 4, stock = stock - 4
where id = 2 and stock - reserved >= 4;

insert into orders_tx
(customer_id, product_id, quantity, status, created_at)


VALUES
(102, 2, 4, 'new', CURRENT_DATE);

COMMIT;

| Поле       | До | Внутри транзакции | После COMMIT |
| ---------- | -: | ----------------: | -----------: |
| `stock`    | 20 |                16 |       **16** |
| `reserved` |  5 |                 9 |        **9** |

| customer_id | product_id | quantity | status |
| ----------: | ---------: | -------: | ------ |
|         102 |          2 |        4 | new    |




Переведи 5000 со счёта id=3 на счёт id=1.

В одной транзакции:

списать 5000 с id=3;
зачислить 5000 на id=1;
создать withdrawal для id=3;
создать deposit для id=1;
обе операции на 5000;
сохранить всё через COMMIT.

BEGIN;

UPDATE accounts
SET balance = balance - 5000
where id = 3 ;

UPDATE accounts
SET balance = balance + 5000
where id = 1 ;


insert into account_operations
(account_id, operation_type, amount, created_at, status)

VALUES
(3, 'withdrawal', 5000, CURRENT_DATE, 'completed');

insert into account_operations
(account_id, operation_type, amount, created_at, status)

VALUES
(1, 'deposit', 5000, CURRENT_DATE, 'completed');


COMMIT;

| account_id | Balance до | Изменение | Balance после |
| ---------: | ---------: | --------: | ------------: |
|          3 |      25000 |     -5000 |     **20000** |
|          1 |      15000 |     +5000 |     **20000** |

| account_id | operation_type | amount | status    |
| ---------: | -------------- | -----: | --------- |
|          3 | withdrawal     |   5000 | completed |
|          1 | deposit        |   5000 | completed |






В одной транзакции выполни последовательно:

Увеличь баланс счёта id=1 на 3000.
Создай для него операцию deposit на 3000.
Измени его статус с active на blocked.
После этого должна произойти ошибка, из-за которой вся транзакция откатывается.

Нужно определить, что останется в базе после ошибки и ROLLBACK.


BEGIN;

UPDATE accounts
SET balance = balance + 3000
where id = 1;


INSERT into account_operations
(account_id, operation_type, amount, created_at, status)

VALUES
(1, 'deposit', 3000, CURRENT_DATE, 'completed');

UPDATE accounts
SET status = 'blocked'
where id = 1;

ROLLBACK;

| Поле              |          До | Внутри транзакции |  После ROLLBACK |
| ----------------- | ----------: | ----------------: | --------------: |
| `balance` счёта 1 |       15000 |             18000 |       **15000** |
| `status` счёта 1  |      active |           blocked |      **active** |
| `deposit` 3000    | отсутствует |            создан | **отсутствует** |





Для заказа orders_tx.id = 5 выполнить отмену.

В одной транзакции:

изменить статус заказа;
освободить резерв товара;
увеличить доступный stock.

Все три изменения должны быть атомарными.

BEGIN;

UPDATE orders_tx
SET status = 'cancelled'
where id = 5;

UPDATE products
SET stock = stock + 5, reserved = reserved - 5
where id = 5;


COMMIT;

| Этап              | Order 5 status  | Product 5 stock | Product 5 reserved |
| ----------------- | --------------- | --------------: | -----------------: |
| До                | `new`           |             100 |                 20 |
| Внутри транзакции | `cancelled`     |             105 |                 15 |
| После COMMIT      | **`cancelled`** |         **105** |             **15** |






Для товара id = 1 создать заказ на 3 единицы.

В одной транзакции:

проверить доступный остаток stock - reserved;
увеличить reserved;
создать заказ;
если доступного количества недостаточно — выполнить ROLLBACK.

BEGIN;


UPDATE products
SET stock = stock - 3 , reserved = reserved + 3
where id = 1 and stock - reserved >= 3;


insert into orders_tx
(customer_id, product_id, quantity, status, created_at)

VALUES

(101, 1, 3, 'new', CURRENT_DATE);

COMMIT;

| Этап              | stock | reserved | order        |
| ----------------- | ----: | -------: | ------------ |
| До                |    10 |        2 | —            |
| Внутри транзакции |     7 |        5 | 3 шт.        |
| После COMMIT      | **7** |    **5** | **сохранён** |




Попытаться перевести 20000 со счёта id = 2.

Перед изменением проверить, что баланс достаточен.

Если недостаточно — изменения не должны сохраняться.


BEGIN;


UPDATE accounts
SET balance = balance - 20000
where id = 2 and balance >= 20000;


ROLLBACK;




Выполнить перевод 4000 со счёта id = 1 на счёт id = 3.

В рамках одной транзакции обеспечить, чтобы:

с первого счёта деньги списались;
на второй счёт деньги зачислились;
для обоих счетов была создана операция.

BEGIN;

UPDATE accounts
SET balance = balance - 4000
where id = 1;


UPDATE accounts
SET balance = balance + 4000
where id = 3;

INSERT INTO account_operations
(account_id, operation_type, amount, created_at, status)
VALUES
(1, 'withdrawal', 4000, CURRENT_DATE, 'completed');

INSERT INTO account_operations
(account_id, operation_type, amount, created_at, status)
VALUES
(3, 'deposit', 4000, CURRENT_DATE, 'completed');


COMMIT;

| Счёт | Balance до | Изменение | Balance после |
| ---: | ---------: | --------: | ------------: |
|    1 |      15000 |     -4000 |     **11000** |
|    3 |      25000 |     +4000 |     **29000** |

| account_id | operation_type | amount | status    |
| ---------: | -------------- | -----: | --------- |
|          1 | withdrawal     |   4000 | completed |
|          3 | deposit        |   4000 | completed |






Выполнить две последовательные транзакции:

Транзакция 1:

увеличить баланс id = 1 на 1000;
COMMIT.

Транзакция 2:

увеличить баланс id = 1 ещё на 2000;
ROLLBACK.

Определить итоговый баланс.

BEGIN;


UPDATE accounts
SET balance = balance + 1000
where id = 1;

COMMIT;


begin; 
UPDATE accounts
SET balance = balance + 2000
where id = 1;


ROLLBACK;

| Этап                       | Balance счёта 1 |
| -------------------------- | --------------: |
| До транзакций              |           15000 |
| После Tx1 `+1000` + COMMIT |           16000 |
| Внутри Tx2 `+2000`         |           18000 |
| После ROLLBACK Tx2         |       **16000** |


Создать транзакцию, в которой:

баланс счёта id = 2 уменьшается на 1000;
создаётся операция withdrawal;
затем выясняется, что операция должна быть отменена.

Отменить все изменения через ROLLBACK.

BEGIN;

update accounts
SET balance = balance - 1000
where id = 2;


insert into account_operations
(account_id, operation_type, amount, created_at, status)
VALUES
(2, 'withdrawal', 1000, CURRENT_DATE, 'completed');

ROLLBACK;

| Этап              | Balance счёта 2 | Операция   | Amount | Status    |
| ----------------- | --------------: | ---------- | -----: | --------- |
| До транзакции     |            8000 | —          |      — | —         |
| Внутри транзакции |            7000 | withdrawal |   1000 | completed |
| После `ROLLBACK`  |        **8000** | —          |      — | —         |




Создать новую операцию deposit для счёта id = 1 на 5000 и одновременно увеличить его баланс на 5000.

Обе операции должны попасть в одну транзакцию.

BEGIN;

insert into account_operations
(account_id, operation_type, amount, created_at, status)
VALUES
(1, 'deposit', 5000, CURRENT_DATE, 'completed');

UPDATE accounts
SET balance = balance + 5000
where id = 1;


COMMIT;

| id | balance |
| -: | ------: |
|  1 |   15000 |


| id |   balance |
| -: | --------: |
|  1 | **20000** |

| account_id | operation_type | amount | status    |
| ---------: | -------------- | -----: | --------- |
|          1 | deposit        |   5000 | completed |





Отменить заказ orders_tx.id = 1.

В одной транзакции:

изменить статус заказа на cancelled;
вернуть зарезервированный товар обратно в stock;
уменьшить reserved.

BEGIN;

UPDATE orders_tx
SET status = 'cancelled'
where id = 1;


UPDATE products
SET stock = stock + 1, reserved = reserved - 1
where id = 1;


COMMIT;

| Объект                   | Было  | Станет      |
| ------------------------ | ----- | ----------- |
| `orders_tx.id=1`         | `new` | `cancelled` |
| `products.id=1.stock`    | 10    | 11          |
| `products.id=1.reserved` | 2     | 1           |





Купить 2 единицы товара id = 2.

В одной транзакции:

уменьшить stock на 2;
увеличить reserved на 2;
создать соответствующий заказ в orders_tx.

После всех изменений выполнить COMMIT.


BEGIN;

UPDATE products
SET stock = stock -2, reserved = reserved + 2
where id = 2;


INSERT into orders_tx
(customer_id, product_id, quantity, status, created_at)
VALUES
(102, 2, 2, 'new', CURRENT_DATE)

COMMIT;

| id | name  | stock | reserved |
| -: | ----- | ----: | -------: |
|  2 | Phone |    20 |        5 |

| id | name  | stock | reserved |
| -: | ----- | ----: | -------: |
|  2 | Phone |    18 |        7 |

| customer_id | product_id | quantity | status |
| ----------: | ---------: | -------: | ------ |
|         102 |          2 |        2 | new    |





В одной транзакции выполнить перевод 3000 со счёта id = 1 на счёт id = 2.

Изменить оба баланса.

Если оба изменения выполнены успешно — COMMIT.

BEGIN;

UPDATE accounts
SET balance = balance - 3000
where id = 1;

UPDATE accounts
SET balance = balance + 3000
where id = 2;


COMMIT;



Начать транзакцию и удалить операцию account_operations с id = 12.

Затем выполнить ROLLBACK.

BEGIN;

delete  from account_operations
where id = 12;

ROLLBACK;



В одной транзакции увеличить stock товара id = 5 на 20 и уменьшить reserved на 5.

Подтвердить изменения.


BEGIN;

UPDATE products
SET stock = stock + 20, reserved = reserved - 5
where id = 5;

COMMIT;



Начать транзакцию и изменить статус счёта id = 4 с blocked на active.

BEGIN;

UPDATE accounts
SET status = 'active'
where id = 4;

COMMIT;




Начать транзакцию, уменьшить баланс счёта id = 2 на 1500, затем отменить транзакцию.

После выполнения баланс должен остаться неизменным.

BEGIN;

UPDATE accounts
SET balance = balance - 1500
where id = 2;

ROLLBACK;



Начать транзакцию, увеличить баланс счёта id = 1 на 2000, затем подтвердить изменения.


BEGIN;

UPDATE accounts
SET balance = balance + 2000
where id = 1;

COMMIT;



~~~
