~~~

общую сумму completed;
количество completed заказов;
средний completed заказ;
место клиента среди всех клиентов по общей сумме;
разделить клиентов на 4 группы одинакового размера по общей сумме.

Вывести только верхнюю группу.


with tt as ( 
    SELECT
    *,
    COALESCE(sum(case when status = 'completed' then amount end) over(PARTITION BY customer_id),0) as r,

    count(case when status = 'completed' then 1 end) over(PARTITION BY customer_id) as r1,

    COALESCE(avg(case when status = 'completed' then amount end) over(PARTITION BY customer_id),0) as r2

    from customers
    LEFT JOIN orders
    on orders.customer_id = customers.id

 ),

 tt1 as (

    SELECT
    DISTINCT
    customer_id,
    r,
    r1,
    r2

    from tt
 ),

 tt2 as (
    select
    *,
    DENSE_RANK() over(ORDER BY r DESC) as r3,

    NTILE(4) over(ORDER BY r DESC) as r4

    from tt1


 )

SELECT

*

from tt2
where r4 = 1;




Для каждого города:

посчитать сумму completed каждого клиента;
посчитать среднюю сумму клиентов города;
оставить клиентов выше среднего;
среди оставшихся определить рейтинг;
вывести топ-3.

Если суммы одинаковые — клиенты должны иметь одинаковый ранг и все попасть в результат.

with tt as ( 
    SELECT
    *,
    COALESCE(sum(case when status ='completed' then amount end),0) as r


    from customers
    LEFT JOIN orders
    on orders.customer_id = customers.id

    GROUP BY city, id, name
 ),

 tt1 as (

        SELECT
        *,
        avg(r) over(PARTITION BY city) as r1


        from tt
 ),

 tt2 as (

    SELECT
    *
    from tt1
    where r>r1

),

tt3 as (

    SELECT
    *,
    RANK() over(PARTITION BY city ORDER BY r DESC) as r2

    from tt2
)

SELECT
*

from tt3
where r2 <= 3;





Для каждого клиента определить:

максимальную сумму заказа;
дату этого максимального заказа;
количество дней, прошедших от максимального заказа до текущего.

Вывести только заказы, которые произошли после максимального заказа.

Если максимальных заказов несколько — считать отправной точкой самый ранний из них.


with tt as ( 
    SELECT
    *,
    max(amount) over(PARTITION BY customer_id) as r,

    case when amount = max(amount) over(PARTITION BY customer_id) then created_at end as r1



    from customers
    LEFT JOIN orders
    on orders.customer_id = customers.id

 ),

 tt1 as (
    SELECT
    *,
    max(r1) over(PARTITION BY customer_id) as r2 

    from tt
 ),

 tt2 as (

    SELECT
    *,
    created_at - r2 as f



    from tt1
 )

SELECT
*

from tt2
where created_at - r2 >0;




Для каждого клиента вывести каждый заказ:

amount;
предыдущий amount;
процент изменения относительно предыдущего заказа;
накопительную сумму заказов;
определить TRUE, если текущий заказ больше предыдущего и одновременно выше накопительного среднего на момент этого заказа.

Для первого заказа процент изменения = 0.

with tt as ( 
    SELECT
    *,
    lag(amount) over(PARTITION BY customer_id ORDER BY created_at) as r,

    sum(amount) over(PARTITION BY customer_id ORDER BY created_at) as r1,

    avg(amount) OVER(PARTITION BY customer_id ORDER BY created_at) as r2

    from customers
    LEFT JOIN orders
    on orders.customer_id = customers.id

 )

 
SELECT
*,
COALESCE((amount::numeric - r ) / NULLIF(r,0)* 100,0) as f,

case when amount > r and amount > r2 then TRUE
else FALSE end as f1


from tt;





Для каждого клиента вывести:

первый заказ (created_at);
последний заказ (created_at);
количество дней между ними;
количество заказов клиента;
общую сумму заказов клиента.

Каждая строка результата должна соответствовать клиенту.

with tt as ( 
    SELECT
    *,
    FIRST_VALUE(amount) over(PARTITION BY customer_id ORDER BY created_at) as r,

    FIRST_VALUE(amount) over(PARTITION BY customer_id ORDER BY created_at DESC) as r1,

    count(orders.id) over(PARTITION BY customer_id) as r2,

    sum(amount) over(PARTITION BY customer_id) as r3,

     FIRST_VALUE(created_at) over(PARTITION BY customer_id ORDER BY created_at) as r4,

       FIRST_VALUE(created_at) over(PARTITION BY customer_id ORDER BY created_at DESC) as r5

    from customers
    LEFT JOIN orders
    on orders.customer_id = customers.id

 )

SELECT

*,

r5 - r4 as t

from tt;



Для каждого города:

посчитать сумму completed каждого клиента;
определить его место в городе;
определить, в какой квартиль по сумме он попадает;
вывести только клиентов из верхней четверти.

with tt as ( 
    SELECT
    *,
    COALESCE(sum(case when status = 'completed' then amount end),0) as r


    from customers
    LEFT JOIN orders
    on orders.customer_id = customers.id

    GROUP BY city, id, name
 ),

 tt1 as (

    SELECT
    *,
    DENSE_RANK() over(PARTITION BY city ORDER BY r DESC) as r1,

    NTILE(4) over(PARTITION BY city ORDER BY r DESC) as r2


    from tt

 )

SELECT
*
from tt1
where r2 =1;




Для каждого города вывести клиентов и:

сумму их completed-заказов;
среднюю сумму клиентов этого города;
разницу между суммой клиента и средней по городу;
место клиента среди клиентов его города по сумме completed.

Затем оставить только клиентов, которые выше среднего по своему городу.


with tt as ( 
    SELECT
    *,
    COALESCE(sum(case when status = 'completed' then amount end),0) as r


    from customers
    LEFT JOIN orders
    on orders.customer_id = customers.id

    GROUP BY city, id, name
 ),

 tt1 as (
    SELECT
    *,
    avg(r) over(PARTITION BY city) as r1,

    RANK() over(PARTITION BY city ORDER BY r DESC) as r3

    from tt


 )

SELECT
*,
r - r1 as r2
from tt1
where r > r1;




Для каждого клиента вывести каждый его заказ и:

порядковый номер заказа клиента;
место заказа по amount среди заказов этого клиента;
сумму всех заказов клиента;
какую долю от общей суммы клиента составляет текущий заказ в процентах.


with tt as ( 
    SELECT
    *,
    ROW_NUMBER() over(PARTITION BY customer_id ORDER BY created_at, id DESC) as r,

    rank() over(PARTITION BY customer_id ORDER BY amount DESC) as r1,

    COALESCE(sum(amount) over(PARTITION BY customer_id),0) as r2


    from customers
    LEFT JOIN orders
    on orders.customer_id = customers.id

 )

SELECT

*,
amount::numeric / r2 * 100 as r3


from tt;




Для каждого клиента вывести каждый заказ и:

среднюю сумму трёх последних заказов, включая текущий;
максимальную сумму среди всех заказов клиента;
разницу между текущим заказом и этим максимумом.


with tt as ( 
    SELECT
    *,
    avg(amount) over(PARTITION BY customer_id ORDER BY created_at ROWS BETWEEN 2 PRECEDING AND CURRENT ROW) as r,


    max(amount) over(PARTITION BY customer_id) as r1

    from customers
    LEFT JOIN orders
    on orders.customer_id = customers.id

 )

SELECT

*,
amount - r1 as r2
 
from tt;





Для каждого клиента вывести каждый заказ и:

предыдущую сумму заказа;
разницу между текущим и предыдущим заказом;
среднюю сумму всех заказов клиента;
определить, был ли текущий заказ выше средней суммы клиента (TRUE/FALSE).

Для первого заказа разницу считать 0.

with tt as ( 
    SELECT
    *,
    lag(amount) over(PARTITION BY customer_id ORDER BY created_at) as r,

    avg(amount) over(PARTITION BY customer_id) as r2


    from customers
    LEFT JOIN orders
    on orders.customer_id = customers.id

 ),

 tt1 as (

    select 
    *,
    case when amount > r2 then TRUE
    else FALSE end as r3


    from tt
 )

SELECT
*,

COALESCE(amount - r,0)

from tt1;





Для каждого клиента вывести:

customer_id
id
created_at
amount
сумму предыдущего заказа;
накопительную сумму заказов клиента, включая текущий.

Для первого заказа предыдущая сумма = 0.


with tt as (
    SELECT
    *,
    lag(amount) over(PARTITION BY customer_id ORDER BY created_at) as r,

    sum(amount) over(PARTITION BY customer_id ORDER BY created_at) as r1

    from customers
    LEFT JOIN orders
    on orders.customer_id = customers.id

)


select
*,
COALESCE(r,0)

from tt;



Для каждого города определить клиента, чья сумма completed выше средней суммы клиентов этого города, а затем среди таких клиентов оставить топ-2 по сумме.

with tt as ( 
    SELECT
    *,
    COALESCE(sum(case when status = 'completed' then amount end),0) as r


    from customers
    LEFT JOIN orders
    on orders.customer_id = customers.id

    GROUP BY city, id, name
 ),

 tt1 as (
    SELECT
    *,
    avg(r) over(PARTITION BY city) as r1

    from tt


 ),

 tt2 as (

SELECT
*

from tt1
where r > r1
 ),

 tt3 as (
    SELECT
    *,
    ROW_NUMBER() over(PARTITION BY city ORDER BY r DESC) as r2


    from tt2
 )

 SELECT
*

 from tt3
 where r2 <= 2;




Для каждого клиента вывести каждый заказ и определить:

customer_id
id
created_at
amount
среднюю сумму двух предыдущих заказов + текущего заказа

То есть для каждого заказа нужно считать среднее по трём последним заказам в последовательности, включая текущий.

with tt as ( 
    SELECT
    *,
    avg(amount) over(PARTITION BY customer_id ORDER BY created_at ROWS BETWEEN 2 PRECEDING AND CURRENT ROW) as r


    from customers
    LEFT JOIN orders
    on orders.customer_id = customers.id

 )

SELECT
*

from tt;




Для каждого клиента определить порядковый номер заказа и одновременно показать:

customer_id
id
created_at
amount
порядковый номер заказа клиента
изменение суммы относительно предыдущего заказа в процентах

Для первого заказа изменение должно быть 0%.

Формула:

(current_amount - previous_amount) / previous_amount × 100

with tt as ( 
    SELECT
    *,
    ROW_NUMBER() over(PARTITION BY customer_id ORDER BY created_at, orders.id DESC) as r,

    lag(amount) over(PARTITION BY customer_id ORDER BY created_at) as r1

    from customers
    LEFT JOIN orders
    on orders.customer_id = customers.id

 )

SELECT
*,
COALESCE((amount - r1)::numeric / NULLIF(r1,0) * 100 ,0) as r3



from tt;




Для каждого клиента определить:

его первый заказ;
его последний заказ;
разницу в днях между первым и последним заказом.

Вывести:

customer_id
first_order_date
last_order_date
days_between


with tt as ( 
    SELECT
    *,
    FIRST VALUE(created_at) over(PARTITION BY customer_id ORDER BY created_at) as r,

    LAST VALUE(created_at) over(PARTITION BY customer_id ORDER BY created_at ROWS BETWEEN UNBOUNDED PRECEDING and UNBOUNDED FOLLOWING) as r1

    from customers
    LEFT JOIN orders
    on orders.customer_id = customers.id

 )

SELECT
*,

r1 - r as ff



from tt;



Для каждого клиента показать:

customer_id
id
created_at
amount
накопительную сумму заказов клиента
долю текущего заказа от общей суммы заказов этого клиента в процентах

with tt as ( 
    SELECT
    *,
    sum(amount) over(PARTITION BY customer_id ORDER BY created_at) as r


    from customers
    LEFT JOIN orders
    on orders.customer_id = customers.id

 )

SELECT
*,
amount::numeric / sum(amount) over(PARTITION BY customer_id) * 100 as r2



from tt;s




Для каждого города найти клиента с максимальной суммой completed.

Если несколько клиентов имеют одинаковую максимальную сумму — вывести всех.

Вывести:

city
name
completed_sum


with tt as ( 
    SELECT
    *,
    COALESCE(sum(case when status = 'completed' then amount end),0) as r


    from customers
    LEFT JOIN orders
    on orders.customer_id = customers.id

    GROUP BY city, id, name
 ),

 tt1 as (
    select 
    *,
    max(r) over(PARTITION BY city) as r1 

    from tt

 ),

 tt2 as (

    SELECT
    *,
    DENSE_RANK() over(PARTITION BY city ORDER BY r DESC) as r2

    from tt1
 )

SELECT
city,
name,
r
from tt2
where r2 = 1;






Для каждого города определить топ-2 клиентов по сумме completed-заказов.

Но теперь есть дополнительное условие:

Если у клиента сумма completed выше средней суммы клиентов его города — он кандидат в топ-2.
Затем среди таких клиентов определить топ-2 по сумме.

Вывести:

city
name
completed_sum
city_avg
rank

Если суммы одинаковые — одинаковое место.


with tt as ( 
    SELECT
    *,
    COALESCE(sum(case when ee1.status = 'completed' then ee1.amount end),0) as r


    from customers e
    LEFT JOIN orders ee1
    on ee1.customer_id = e.id

    GROUP BY e.city, e.id, e.name
 ),

 tt1 as (
    SELECT
    *,
    RANK() over(PARTITION BY city ORDER BY r DESC) as r1

    from tt

    where r > (
        SELECT
        avg(r3)
        from (
            SELECT
            COALESCE(sum(case when ee1.status = 'completed' then ee1.amount end),0) as r3

            from customers e1
            LEFT JOIN orders ee1
            on ee1.customer_id = e1.id 

            where e1.city = e.city

            GROUP BY ee1.customer_id
        ) cc
    )


 )

SELECT

city,
name,
r,
r1,

(
        SELECT
        avg(r3)
        from (
            SELECT
            COALESCE(sum(case when ee1.status = 'completed' then ee1.amount end),0) as r3

            from customers e1
            LEFT JOIN orders ee1
            on ee1.customer_id = e1.id 

            where e1.city = e.city

            GROUP BY ee1.customer_id
        ) cc
    ) as r5


from tt1
where r1<=2;

| city   | name   |      r | city_avg | rank |
| ------ | ------ | -----: | -------: | ---: |
| Москва | Пётр   | 18 000 |   15 000 |    1 |
| Уфа    | Сергей | 12 000 |   10 000 |    1 |






Для каждого заказа показать:

customer_id
id
created_at
amount
накопительную сумму заказов клиента
накопительную сумму только completed-заказов клиента


with tt as ( 
    SELECT
      *,
     sum(amount) over(PARTITION BY customer_id ORDER BY created_at) as r1,

     sum(case when status = 'completed' then amount end) over(PARTITION BY customer_id ORDER BY created_at) as r2



    from customers
    LEFT JOIN orders
    on orders.customer_id = customers.id


 )

SELECT

*


from tt;


Для каждого города вывести только клиента, занявшего 1-е место по сумме completed-заказов.

Вывести:

city;
name;
сумму completed;
место.

with tt as ( 
    SELECT
    *,
    COALESCE(sum(case when status = 'completed' then amount end),0) as r


    from customers
    LEFT JOIN orders
    on orders.customer_id = customers.id

    GROUP BY city, id, name

 ),

 tt1 as (
    SELECT
    *,
    ROW_NUMBER() over(PARTITION BY city ORDER BY r DESC) as r1

    from tt
 
 )

SELECT
city,
name,
r,
r1

from tt1
where r1 = 1;

| city            | name    |     r | r1 |
| --------------- | ------- | ----: | -: |
| Москва          | Пётр    | 18000 |  1 |
| Уфа             | Ольга   | 15000 |  1 |
| Казань          | Елена   | 17000 |  1 |
| Санкт-Петербург | Николай | 20000 |  1 |




Вывести:

city;
name;
сумму completed;
место клиента.

 with tt as ( 
    SELECT
    *,
    COALESCE(sum(case when status = 'completed' then amount end),0) as r



    from customers
    LEFT JOIN orders
    on orders.customer_id = customers.id

    GROUP BY city, id, name
 ),

 tt1 as (
    SELECT
    *,
    DENSE_RANK() over(PARTITION BY city ORDER BY r DESC) as r1
    from tt
 )


 SELECT
city,
name,
r,
r1
 from tt1;





Для каждого города пронумеровать клиентов по сумме их completed-заказов.

Вывести:

city;
name;
сумму completed-заказов;
номер клиента внутри города.

1-е место должен получить клиент с самой большой суммой completed.

 with tt as ( 
    SELECT
    *,
    COALESCE(sum(case when status = 'completed' then amount end),0) as r



    from customers
    LEFT JOIN orders
    on orders.customer_id = customers.id

    GROUP BY city, id, name
 ),

 tt1 as (
    select
    *,
    ROW_NUMBER() over(PARTITION BY city ORDER BY r DESC) as r1

    from tt

 )

 SELECT
city,
name,
r,
r1

 from tt1;


Для каждого города посчитать общую сумму всех заказов.

Вывести:

city;
общую сумму заказов.

Условие: 
показать только города, где общая сумма заказов больше 20 000.


SELECT
city,

COALESCE(sum(case when amount is not null then amount end),0) as r

from customers
LEFT JOIN orders
on orders.customer_id = customers.id

GROUP BY city

HAVING COALESCE(sum(case when amount is not null then amount end),0) > 20000;





Для каждого города найти клиента с максимальной суммой completed, а также вывести:

город;
имя;
сумму completed;
среднюю сумму completed по городу;
разницу между суммой клиента и средней.


with tt as (

SELECT
city,
id,
name,


COALESCE(sum(case when status = 'completed' then amount end),0) as r



from customers
LEFT JOIN orders
on orders.customer_id = customers.id


GROUP BY city, id, name

)


SELECT
*,
max(r) over(PARTITION by city) as c1,
avg(r) over(PARTITION BY city) as c2,

r - avg(r) over(PARTITION BY city) as c3
from tt;

| city   | name    |     r |    c1 |       c2 |       c3 |
| ------ | ------- | ----: | ----: | -------: | -------: |
| Москва | Иван    | 12000 | 18000 | 15333.33 | -3333.33 |
| Москва | Пётр    | 18000 | 18000 | 15333.33 |  2666.67 |
| Москва | Анна    | 16000 | 18000 | 15333.33 |   666.67 |
| Уфа    | Сергей  |  7000 | 15000 |  9666.67 | -2666.67 |
| Уфа    | Ольга   | 15000 | 15000 |  9666.67 |  5333.33 |
| Уфа    | Дмитрий |  7000 | 15000 |  9666.67 | -2666.67 |





Найти клиентов, у которых одновременно:

возраст выше среднего возраста их города;
есть completed;
сумма completed больше 15 000;
количество completed выше среднего количества completed клиентов их города.

Вывести также все три рассчитанных средних/пороговых значения.


SELECT
e.id,
e.name,
e.city,
e.age,

(
    SELECT
    avg(e1.age)

    from customers e1

    where e.city = e1.city
) as avg_age,

count(case when ee.status = 'completed' then 1 end) as count,



(
    SELECT
    avg(r)
    from (
        select
        count(case when ee1.status = 'completed' then 1 end) as r

        from customers e2
        LEFT JOIN orders ee1
        on ee1.customer_id = e2.id 

        where e2.city = e.city

        GROUP BY ee1.customer_id
) as avg_count,

COALESCE(sum(case when ee.status = 'completed' then ee.amount end),0) as c2

from customers e
LEFT JOIN orders ee
on ee.customer_id = e.id

where e.age > (
    SELECT
    avg(e1.age)

    from customers e1

    where e.city = e1.city
)

HAVING count(case when ee.status = 'completed' then 1 end)>= 1 and COALESCE(sum(case when ee.status = 'completed' then ee.amount end),0)> 15000 and count(case when ee.status = 'completed' then 1 end)> (
    SELECT
    avg(r)
    from (
        select
        count(case when ee1.status = 'completed' then 1 end) as r

        from customers e2
        LEFT JOIN orders ee1
        on ee1.customer_id = e2.id 

        where e2.city = e.city

        GROUP BY ee1.customer_id
    ) tt
);

| city   | name  | age | avg_age | completed | sum_completed | avg_completed |
| ------ | ----- | --: | ------: | --------: | ------------: | ------------: |
| Казань | Елена |  41 |   33.67 |         2 |         17000 |          1.67 |






Найти клиентов, которые входят в топ-3 своего города по сумме completed, но при этом имеют хотя бы один cancelled-заказ.

with tt as (

SELECT
id,
name,

ROW_NUMBER() over(PARTITION BY city ORDER BY COALESCE(sum(case when status = 'completed' then amount end),0) DESC) as r

from customers
LEFT JOIN orders
on orders.customer_id = customers.id


GROUP BY city, id, name

HAVING count(case when status = 'cancelled' then 1 end ) >= 1

)


SELECT
id,
name,
r

from tt
where r <= 3;

| city            | name    | completed |  r |
| --------------- | ------- | --------: | -: |
| Москва          | Иван    |     12000 |  3 |
| Москва          | Анна    |     16000 |  2 |
| Уфа             | Ольга   |     15000 |  1 |
| Уфа             | Дмитрий |      7000 |  3 |
| Казань          | Алексей |     11000 |  2 |
| Казань          | Елена   |     17000 |  1 |
| Казань          | Мария   |      7000 |  3 |
| Санкт-Петербург | Николай |     20000 |  1 |





Для каждого клиента определить его позицию внутри города по сумме completed:

1 — лучший результат;
остальные — ниже.

Использовать оконную функцию.


with tt as (

SELECT
*,
ROW_NUMBER() over(PARTITION BY city ORDER BY COALESCE(sum(case when status = 'completed' then amount end),0) DESC) as r

from customers
LEFT JOIN orders
on orders.customer_id = customers.id


GROUP BY city, id, name



)


SELECT
*

from tt;

| city            | name    | completed |     r |
| --------------- | ------- | --------: | ----: |
| Москва          | Пётр    |    18 000 | **1** |
| Москва          | Анна    |    16 000 |     2 |
| Москва          | Иван    |    12 000 |     3 |
| Уфа             | Ольга   |    15 000 | **1** |
| Уфа             | Сергей  |     7 000 |     2 |
| Уфа             | Дмитрий |     7 000 |     3 |
| Казань          | Елена   |    17 000 | **1** |
| Казань          | Алексей |    11 000 |     2 |
| Казань          | Мария   |     7 000 |     3 |
| Санкт-Петербург | Николай |    20 000 | **1** |





Для каждого клиента вывести:

имя;
количество completed;
количество cancelled;
долю completed-заказов от общего количества его заказов.


with tt as (

SELECT
id
name

count(case when status = 'completed' then 1 end) as r,


count(case when status = 'cancelled' then 1 end) as r1,

count(case when status = 'completed' then 1 end)::numeric / count(*) * 100 as r2

from customers
LEFT JOIN orders
on orders.customer_id = customers.id


GROUP BY id, name;

| name    | completed | cancelled | всего заказов | доля completed |
| ------- | --------: | --------: | ------------: | -------------: |
| Иван    |         2 |         1 |             3 |         66.67% |
| Пётр    |         2 |         0 |             2 |           100% |
| Анна    |         2 |         1 |             3 |         66.67% |
| Сергей  |         2 |         0 |             2 |           100% |
| Ольга   |         2 |         1 |             3 |         66.67% |
| Дмитрий |         1 |         1 |             2 |            50% |
| Мария   |         2 |         0 |             2 |           100% |
| Алексей |         1 |         1 |             2 |            50% |
| Елена   |         2 |         1 |             3 |         66.67% |
| Николай |         1 |         1 |             2 |            50% |




Для каждого клиента вывести:

заказ;
amount;
накопительную сумму заказов;
общую сумму заказов клиента.



with tt as (

SELECT
id,
customer_id,
amount,
sum(amount) over(PARTITION BY customer_id ORDER BY created_at) as r ,

sum(amount) over(PARTITION BY customer_id) as r1
from orders


)


SELECT
*

from tt;




Для каждого клиента вывести только completed-заказы и показать накопительную сумму completed по мере совершения заказов.


with tt as (

SELECT
*,


sum(case when status = 'completed' then amount end) over(PARTITION BY customer_id ORDER BY created_at) as r

from orders

where status = 'completed'
)


SELECT
*

from tt;



Для каждого клиента показать:

текущий заказ;
следующий заказ;
сумму следующего заказа.

with tt as (

SELECT
*,
LEAD(id) over(PARTITION BY customer_id ORDER BY created_at) as r1,
LEAD(amount) over(PARTITION BY customer_id ORDER BY created_at) as r


from orders


)


SELECT
*

from tt;




Для каждого клиента вывести его заказы в порядке created_at и предыдущий заказ этого же клиента.

Вывести:

customer_id;
id;
created_at;
amount;
сумму предыдущего заказа.



with tt as (
    
SELECT
*,
lag(amount) over(PARTITION BY customer_id ORDER BY created_at ) as r 

from customers
LEFT JOIN orders
on orders.customer_id = customers.id 

)


SELECT

id,
customer_id,
created_at,
amount,
r

from tt;




Для каждого клиента присвоить ROW_NUMBER() по сумме completed-заказов внутри города.

Вывести только клиентов, которые занимают первое место в своём городе.


SELECT
id,
name,
city,
r1
from (
    SELECT
    *,
    COALESCE(sum(case when status= 'completed' then amount end),0) as r1,

    ROW_NUMBER() over(PARTITION BY city ORDER BY COALESCE(sum(case when status= 'completed' then amount end),0) DESC ) as r

    from customers
    LEFT JOIN orders
    on orders.customer_id = customers.id

    GROUP BY city, id, name
) tt

where r = 1;




--------------------------------------------------------------------------------------------------------------------

Для каждого клиента присвоить ранг по сумме completed-заказов внутри его города.

Вывести:

city;
name;
сумму completed;
ранг.

SELECT
id,
city,
name,


COALESCE(sum(case when status = 'completed' then amount end),0) as r,

rank() over(PARTITION BY city ORDER BY COALESCE(sum(case when status = 'completed' then amount end),0) DESC) as r1


from customers
LEFT JOIN orders
on orders.customer_id = customers.id

GROUP BY city, name, id;

| id | city   | name   | r — completed | r1 — rank |
| -: | ------ | ------ | ------------: | --------: |
|  2 | Москва | Пётр   |        23 000 |     **1** |
|  3 | Москва | Анна   |        15 000 |     **2** |
|  1 | Москва | Иван   |        12 000 |     **3** |
|  4 | Уфа    | Сергей |        18 000 |     **1** |
|  5 | Уфа    | Олег   |         9 000 |     **2** |


--------------------------------------------------------------------------------------------------------------------

самый старший клиент получает №1.

Вывести:

city;
name;
age;
номер клиента.

SELECT

city,
name,
age,
ROW_NUMBER() over(PARTITION BY city ORDER BY age DESC) as r


from customers;

| name  | city   | age |  r |
| ----- | ------ | --: | -: |
| Иван  | Москва |  50 |  1 |
| Пётр  | Москва |  40 |  2 |
| Анна  | Москва |  30 |  3 |
| Олег  | Уфа    |  45 |  1 |
| Мария | Уфа    |  35 |  2 |



--------------------------------------------------------------------------------------------------------------------


Для каждого клиента вывести:

имя;
город;
сумму его completed-заказов;
сумму completed-заказов всех клиентов его города.

SELECT
DISTINCT
e.name,
e.city,

COALESCE(sum(case when ee.status = 'completed' then ee.amount end) over(PARTITION BY ee.customer_id),0) as r,

(
    select 
    sum(r1) as r2
    from (
        SELECT
        COALESCE(sum(case when ee1.status = 'completed' then ee1.amount end),0) as r1

        from customers e1
        LEFT JOIN orders ee1
        on ee1.customer_id = e1.id

        where e1.city = e.city

        GROUP BY ee1.customer_id
    ) tt
)

from customers e
LEFT JOIN orders ee
on ee.customer_id = e.id;

--------------------------------------------------------------------------------------------------------------------

Для каждого клиента вывести его имя, город и возраст, а также средний возраст клиентов его города.

SELECT

e1.id,
e1.name,
e1.city,
e1.age,

(select
    avg(e.age) as r1

    from customers e

    where e.city = e1.city
)

from customers e1
LEFT JOIN orders
on orders.customer_id = e1.id;


--------------------------------------------------------------------------------------------------------------------

Для каждого клиента вывести:

id;
name;
city;
сумму completed-заказов клиента;
среднюю сумму completed-заказов среди всех клиентов.

SELECT

id,
name,
city,

COALESCE(sum(case when status = 'completed' then amount end) over(PARTITION BY customer_id),0) as r,

(
    select 
    COALESCE(avg(r),0) as r1
    from (
        SELECT
        COALESCE(sum(case when status = 'completed' then amount end),0) as r

        from orders

        GROUP BY customer_id
    ) tt
)

from customers
LEFT JOIN orders
on orders.customer_id = customers.id;

--------------------------------------------------------------------------------------------------------------------

Для каждого заказа вывести:

id;
customer_id;
amount;
сумму заказов этого клиента;
разницу между amount текущего заказа и суммой заказов клиента.

SELECT


id,
customer_id,
amount,
sum(amount) over(PARTITION BY customer_id) as r,
amount - sum(amount) over(PARTITION BY customer_id) as r1

from orders;

--------------------------------------------------------------------------------------------------------------------


Для каждого клиента вывести:

id;
name;
city;
количество его заказов;
общее количество заказов всех клиентов.


SELECT

id,
name,
city,
count(*) over(PARTITION BY customer_id) as r,
count(orders.id) over() as r1

from customers
LEFT JOIN orders
on orders.customer_id = customers.id;

--------------------------------------------------------------------------------------------------------------------



Для каждого заказа вывести его amount и сумму всех заказов этого клиента.

SELECT

id,
amount,
sum(amount) over(PARTITION BY customer_id)

from orders;

| id | amount | сумма клиента |
| -: | -----: | ------------: |
|  1 |   5000 |         14000 |
|  2 |   7000 |         14000 |
|  3 |   2000 |         14000 |



Найти самую большую транзакцию каждого клиента, но вернуть только одну строку, даже если у клиента несколько одинаковых максимальных сумм.


with tt as (
    SELECT
    *,
    row_number() over(PARTITION BY customer_id ORDER BY amount DESC, date, id) as r 
    from sales
)
SELECT
*
from tt
where r = 1;


> Нужно получить предыдущую продажу для каждого клиента отдельно.

| date  | customer_id | amount |
| ----- | ----------: | -----: |
| 01.08 |           1 |    100 |
| 02.08 |           1 |    150 |
| 03.08 |           1 |    120 |
| 01.08 |           2 |    500 |
| 02.08 |           2 |    450 |
| 03.08 |           2 |    600 |



with tt as (
    select
    *,
    lag(amount) over(PARTITION BY customer_id ORDER BY date) as r
    from sales
)

select *
from tt;





Нужно найти последнюю продажу каждого клиента.


with tt as (
    select
    *,
    row_number() over(PARTITION BY customer_id ORDER BY date DESC) as r
    from sales
)

select 
*
from tt
where r = 1;



Задача

Нужно получить самую последнюю транзакцию каждого клиента.

Но есть условие:

Если у клиента несколько транзакций в одну и ту же дату, нам нужна только одна, с самым большим transaction_id.


| transaction_id | customer_id | date  | amount |
| -------------: | ----------: | ----- | -----: |
|            101 |           1 | 03.08 |    120 |
|            102 |           1 | 03.08 |    200 |
|            103 |           1 | 02.08 |    150 |
|            104 |           1 | 01.08 |    100 |
|            201 |           2 | 03.08 |    500 |
|            202 |           2 | 02.08 |    450 |
|            203 |           2 | 01.08 |    400 |



with tt as (
    select
    *,
    row_number() over(PARTITION BY customer_id ORDER BY date DESC, transaction_id DESC) as r
    from sales
)

select
*
from tt 
where r=1;



ROWS BETWEEN

скользящая сумма 

| date  | customer_id | amount |
| ----- | ----------: | -----: |
| 01.08 |           1 |    100 |
| 02.08 |           1 |    150 |
| 03.08 |           1 |    120 |
| 04.08 |           1 |    200 |
| 05.08 |           1 |    180 |


with tt as (
    select
    *,
    sum(amount) over(ORDER BY date ROWS BETWEEN 2 PRECEDING and CURRENT ROW) as roll
    from sales

)

select
*
from tt;


Нужно добавить столбец:
>> 
>> rolling_avg
>> 
>> Он должен показывать среднее значение текущей строки и двух предыдущих.


with tt as (
    select
    *,
    avg(amount) over(ORDER BY date ROWS BETWEEN 2 PRECEDING and CURRENT ROW) as r
    from sales
)

select 
*
from tt;




Задача №12

Для каждого клиента нужно показать:

его максимальную сумму продажи, но при этом сохранить каждую исходную строку.


| id | customer_id | date       | amount |
| -: | ----------: | ---------- | -----: |
|  1 |           1 | 2026-08-01 |    100 |
|  2 |           1 | 2026-08-02 |    150 |
|  3 |           1 | 2026-08-03 |    120 |
|  4 |           2 | 2026-08-01 |    500 |
|  5 |           2 | 2026-08-02 |    450 |
|  6 |           2 | 2026-08-03 |    600 |


with tt as (
    select
    *,
    max(amount) over(PARTITION BY customer_id) as r
    from sales
)

select 
*
from tt;



Теперь нужно найти на сколько текущая продажа отличается от максимальной продажи клиента.

with tt as (
    select
    *,
    max(amount) over(PARTITION BY customer_id) as r
    from sales
)

select 
*,
amount - r as r1
from tt;


Теперь нужно определить, какая продажа является максимальной у клиента.

Добавь столбец:

is_max

Он должен содержать:

1 → если amount = максимальной продаже клиента
0 → если нет


with tt as (
    select
    *,
    max(amount) over(PARTITION BY customer_id) as r
    from sales
),


tt1 as (
    SELECT
    *,
    case 
    WHEN amount = r then 1
    else 0
    end as r1
    from tt
)

select 
*

from tt1;



 Нужно получить:
>> 
>> customer_id
>> amount
>> max_amount
>> percent_of_max
>> 
>> Например, для клиента 1:
>> 
>> 100 / 150 * 100 = 66.67%
>> 150 / 150 * 100 = 100%
>> 120 / 150 * 100 = 80%

with tt as (
    SELECT
    *,
    max(amount) over(PARTITION BY customer_id) as r
    from sales
)

select 
customer_id,
amount,
r,
(amount / r::numeric)*100 as r1
from tt;




Теперь сделаем сравнение с предыдущей продажей в процентах.

Для каждой строки нужно вывести:

customer_id
date
amount
previous_amount
change_percent

Формула:

(amount - previous_amount) / previous_amount * 100


with tt as (
    SELECT
    *,
    lag(amount) over(PARTITION BY customer_id ORDER BY date) as r
    from sales
)

select 
customer_id,
date,
amount,
r,
(amount::numeric - r) / nullif(r,0) * 100 as r1
from tt;



накопительная сумма


with tt as (
    select 
    *,
    sum(amount) over(PARTITION BY customer_id ORDER BY  date) as r
    from sales
)

SELECT
*
from tt;




теперь накопительная не по клиенту, а по всей таблице


with tt as (
    select 
    *,
    sum(amount) over(ORDER BY  date, id) as r
    from sales
)

SELECT
*
from tt;



Он должен показывать, сколько транзакций накопилось у клиента к каждой дате.


with tt as (
    select 
    *,
    count(*) over(PARTITION BY customer_id ORDER BY date, id) as r
    from sales
)

SELECT
*
from tt;



Следующая задача

Теперь сделаем скользящее среднее.

Нужно для каждой транзакции посчитать среднее значение amount:

текущая транзакция + две предыдущие

with tt as (
    select 
    *,
    avg(amount) over(PARTITION BY customer_id ORDER BY date, id ROWS BETWEEN 2 PRECEDING and current row) as r
    from sales
)
select
*
from tt;


Теперь попробуем MIN() и MAX() со скользящим окном.

Для каждой транзакции нужно найти максимальную сумму среди текущей и двух предыдущих транзакций этого клиента.


with tt as (
    select 
    *,
    max(amount) over(PARTITION BY customer_id ORDER BY date, id ROWS BETWEEN BETWEEN 2 PRECEDING AND CURRENT ROW) as r
from sales
)
select
*
from tt;



FILTER с оконными функциями.


Нужно для каждого клиента посчитать:

сколько у него было транзакций на сумму больше 200.

При этом мы хотим оставить все исходные строки.





with tt as (
    select 
    *,
    count(*) filter(where amount > 200) over(PARTITION by customer_id)
)
select
*
from tt;



Нужно получить накопительный максимум для каждого клиента.

with tt as (
    SELECT
    *,
    max(amount) over(PARTITION BY customer_id ORDER BY date, id) as r
    from sales
)
SELECT
*
from tt;


Для каждого клиента нужно определить:

какая минимальная сумма была у клиента от первой транзакции до текущей.

with tt as (
    SELECT
    *,
    min(amount) over(PARTITION BY customer_id ORDER BY date, id) as r
    from sales
)
SELECT
*
from tt;



Теперь сделаем чуть интереснее.
>> 
>> Нужно для каждого клиента посчитать накопительную сумму только тех транзакций, где amount > 200.

with tt as (
    SELECT
    *,
    sum(amount) filter(where amount > 200) over(PARTITION BY customer_id)
    from sales
)
SELECT
*
from tt;


Для каждой строки посчитать:

сумму только тех транзакций, где amount > 200, среди текущей и двух предыдущих строк.

То есть окно всегда 3 строки.

with tt as (
    SELECT
    *,
    sum(amount) filter(where amount > 200) over( order by date, id ROWS BETWEEN 2 PRECEDING AND CURRENT ROW) as r
    from sales
)
SELECT
*
from tt;
~~~
