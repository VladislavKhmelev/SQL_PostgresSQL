~~~

CREATE TABLE dml_clients_5 (
    id INT PRIMARY KEY,
    name TEXT NOT NULL,
    city TEXT NOT NULL
);

CREATE TABLE dml_orders_5 (
    id INT PRIMARY KEY,
    client_id INT NOT NULL,
    amount NUMERIC(10,2) NOT NULL,
    status TEXT NOT NULL
);

Задача:
Увеличь сумму заказа на 10%, если заказ принадлежит клиенту из 'Москва'.

CREATE TABLE dml_clients_5 (
    id INT PRIMARY KEY,
    name TEXT NOT NULL,
    city TEXT NOT NULL
);

CREATE TABLE dml_orders_5 (
    id INT PRIMARY KEY,
    client_id INT NOT NULL,
    amount NUMERIC(10,2) NOT NULL,
    status TEXT NOT NULL
);


update dml_orders_5
set amount = amount + (amount*0.1)
where EXISTS (
    SELECT 1
    from dml_clients_5
    where dml_clients_5.id = dml_orders_5.client_id
    and city = 'Moscow'
);




UPDATE + RETURNING

Структура:

(id, name, balance) dml_accounts_5
CREATE TABLE dml_accounts_5 (
    id INT PRIMARY KEY,
    name TEXT NOT NULL,
    balance NUMERIC(12,2) NOT NULL
);

CREATE TABLE dml_accounts_5 (
    id INT PRIMARY KEY,
    name TEXT NOT NULL,
    balance NUMERIC(12,2) NOT NULL
);


UPDATE dml_accounts_5
SET balance = balance + 1000
where balance <0 

RETURNING id, name, balance;



ON CONFLICT

Структура:

(id, sku, stock, price) dml_inventory_5
CREATE TABLE dml_inventory_5 (
    id INT PRIMARY KEY,
    sku TEXT UNIQUE NOT NULL,
    stock INT NOT NULL,
    price NUMERIC(10,2) NOT NULL
);

Задача:
Добавь товар в dml_inventory_5.
Если такой sku уже существует — обнови его stock и price значениями из новой записи.

CREATE TABLE dml_inventory_5 (
    id INT PRIMARY KEY,
    sku TEXT UNIQUE NOT NULL,
    stock INT NOT NULL,
    price NUMERIC(10,2) NOT NULL
);

insert into dml_inventory_5
(sku, stock, price)

VALUES

('SKU004', 25, 45000)

on CONFLICT (sku)
do UPDATE
SET
stock = excluded.stock,
price = excluded.price;






Создай:

dml_logs_3

id
user_id
created_at

Через CTE найди записи старше определённой даты и удали их.

CREATE TABLE dml_logs_3 (
    id INT PRIMARY KEY,
    user_id INT NOT NULL,
    created_at TIMESTAMP NOT NULL
);


delete from dml_logs_3 e
where EXISTS (
    select 1
    from dml_logs_3 e1
    where e1.id = e.id
    and e1.created_at < date '2026-01-01'
);




Создай:

dml_customers_3

id
name
bonus

dml_orders_3

id
customer_id
amount

Установи bonus клиента равным 5% от общей суммы его заказов.

CREATE TABLE dml_customers_3 (
    id INT PRIMARY KEY,
    name TEXT NOT NULL,
    bonus NUMERIC(10,2) DEFAULT 0
);

CREATE TABLE dml_orders_3 (
    id INT PRIMARY KEY,
    customer_id INT NOT NULL,
    amount NUMERIC(10,2) NOT NULL
);


UPDATE dml_customers_3
set bonus = (
    select 
    sum(amount) * 0.05
    from dml_orders_3
    where dml_orders_3.customer_id = dml_customers_3.id
);




Создай:

dml_sales_3

id
product_id
quantity

dml_products_3

id
name
price

dml_sales_report_3

product_id
product_name
total_amount

Перенеси в dml_sales_report_3 данные через INSERT ... SELECT, рассчитав:

quantity × price

CREATE TABLE dml_sales_3 (
    id INT PRIMARY KEY,
    product_id INT NOT NULL,
    quantity INT NOT NULL
);

CREATE TABLE dml_products_3 (
    id INT PRIMARY KEY,
    name TEXT NOT NULL,
    price NUMERIC(10,2) NOT NULL

);

CREATE TABLE dml_sales_report_3 (
    product_id INT,
    product_name TEXT,
    total_amount NUMERIC(12,2)
);

INSERT INTO dml_sales_report_3
(product_id, product_name, total_amount)

select

product_id, name, sum(quantity * price)

from dml_sales_3
inner JOIN dml_products_3
on dml_sales_3.product_id = dml_products_3.id


GROUP BY product_id, name;




Удалить из dml_products_3 товары, которые ни разу не встречались в таблице dml_order_items_3.

Таблицы:

dml_products_3
id
name
price
dml_order_items_3
id
product_id
quantity


CREATE TABLE dml_products_3 (
    id INT PRIMARY KEY,
    name TEXT NOT NULL,
    price NUMERIC(10,2) NOT NULL
);


CREATE TABLE dml_order_items_3 (
    id INT PRIMARY KEY,
    product_id INT NOT NULL,
    quantity INT NOT NULL
);

DELETE from dml_products_3
where not EXISTS (
    select 1
    from dml_order_items_3
    where dml_order_items_3.product_id = dml_products_3.id
);




Создай:

dml_customers_3

id
name

dml_orders_3

id
customer_id
amount

Удалить клиентов, у которых нет ни одного заказа.


CREATE TABLE dml_customers_3 (
    id INT PRIMARY KEY,
    name TEXT NOT NULL
);


CREATE TABLE dml_orders_3 (
    id INT PRIMARY KEY,
    customer_id INT NOT NULL,
    amount NUMERIC(10,2) NOT NULL
);

delete from dml_customers_3
where not EXISTS (
    select 1
    from dml_orders_3
    where dml_orders_3.customer_id = dml_customers_3.id
);



В тех же таблицах установи статус заказа:

amount >= 10000 → 'vip'
amount >= 5000 → 'large'
иначе → 'regular'

UPDATE dml_orders_3
SET status = 
case when amount >= 10000 then 'vip'
when amount >= 5000 then 'large'
else 'regular' end;





Создай:

dml_clients

id
name
city

dml_orders_3

id
client_id
amount
status

Увеличь на 10% сумму заказов клиентов из города 'Moscow'.

CREATE TABLE dml_clients (
    id INT PRIMARY KEY,
    name TEXT NOT NULL,
    city TEXT NOT NULL
);

CREATE TABLE dml_orders_3 (
    id INT PRIMARY KEY,
    client_id INT NOT NULL,
    amount NUMERIC(10,2) NOT NULL,
    status TEXT NOT NULL
);

UPDATE dml_orders_3
SET amount = amount + (amount*0.1)
from dml_clients
where dml_orders_3.client_id = dml_clients.id
and city = 'Moscow';



В dml_employees увеличь зарплату на 10% сотрудникам со статусом 'middle'.

Верни:

id
name
salary
status

UPDATE dml_employees
SET salary = salary + (salary*0.1)
where status = 'middle'

returning
id,
name,
salary,
status;




Для dml_inventory выполни UPSERT, используя EXCLUDED, чтобы при конфликте:

stock = EXCLUDED.stock
price = EXCLUDED.price

CREATE TABLE dml_inventory (
    id INT PRIMARY KEY,
    sku TEXT UNIQUE NOT NULL,
    name TEXT NOT NULL,
    stock INT NOT NULL,
    price NUMERIC(10,2) NOT NULL
);


insert into dml_inventory
(id, sku, name, stock, price)
VALUES

(3, 'SKU003', 'Клавиатура', 15, 5000)
on conflict (sku)
do UPDATE
SET
stock = EXCLUDED.stock,
price = EXCLUDED.price;





Создай:

dml_inventory

id
sku
name
stock
price

sku должен быть уникальным.

Выполни UPSERT:

если sku отсутствует → добавить товар;
если sku уже существует → обновить stock и price.

CREATE TABLE dml_inventory (
    id INT PRIMARY KEY,
    sku TEXT UNIQUE NOT NULL,
    name TEXT NOT NULL,
    stock INT NOT NULL,
    price NUMERIC(10,2) NOT NULL
);

insert into tt 
(id, sku, name, stock, price)
VALUES

(1, 'SKU001', 'Ноутбук', 10, 75000)

on CONFLICT (sku)
do UPDATE 
set 
stock =EXCLUDED.stock,
price =EXCLUDED.price;





Создай:

dml_customers

id
name
city
status

и:

dml_customers_archive

id
name
city
status

Перенеси в dml_customers_archive через INSERT ... SELECT только клиентов со статусом 'inactive'.


CREATE TABLE dml_customers (
    id INT PRIMARY KEY,
    name TEXT NOT NULL,
    city TEXT NOT NULL,
    status TEXT NOT NULL
);

CREATE TABLE dml_customers_archive (
    id INT PRIMARY KEY,
    name TEXT NOT NULL,
    city TEXT NOT NULL,
    status TEXT NOT NULL
);


INSERT into dml_customers_archive
(id, name, city, status)
select
id,
name,
city,
status

from dml_customers
where status = 'inactive';



Создай таблицу dml_employees:

id
name
salary
status

Измени status:

salary < 50000 → 'junior'
salary 50000–100000 → 'middle'
salary > 100000 → 'senior'

| id | name    |    salary | status |
| -: | ------- | --------: | ------ |
|  1 | Алексей |  45000.00 | new    |
|  2 | Марина  |  75000.00 | new    |
|  3 | Иван    | 120000.00 | new    |
|  4 | Ольга   |  55000.00 | new    |

| id | name    |    salary | status |
| -: | ------- | --------: | ------ |
|  1 | Алексей |  45000.00 | junior |
|  2 | Марина  |  75000.00 | middle |
|  3 | Иван    | 120000.00 | senior |
|  4 | Ольга   |  55000.00 | middle |




Добавь нового пользователя в dml_users и верни:

id
name
email

INSERT into dml_users 
(id, name, email, age)
VALUES
(4, 'Ольга', 'olga@mail.ru', 28)

RETURNING
id, name, email;



В таблице dml_products увеличь цену конкретного товара на 500 и верни:

id
name
price

UPDATE dml_products
SET price = price + 500
where id = 4

RETURNING 
id,
name,
price;




В таблице dml_products удали товар с конкретным id.

DELETE from dml_products
where id = 2;

| id | name       | price |
| -: | ---------- | ----: |
|  1 | Ноутбук    | 82500 |
|  3 | Клавиатура |  5500 |
|  4 | Мышь       |  2750 |



В таблице dml_products увеличь price на 20% только для товаров, цена которых меньше 1000.


UPDATE dml_products
SET price = price + (price*0.2)
where price < 1000;




В таблице dml_products увеличь price на 10% для всех товаров.

UPDATE dml_products
SET price = price + (price*0.1);





В таблицу dml_products:

id
name
price

Вставь минимум 4 товара одним запросом.


INSERT into dml_products ()
(id, name, price)
VALUES

(1, 'Ноутбук', 75000),
(2, 'Монитор', 30000),
(3, 'Клавиатура', 5000),
(4, 'Мышь', 2500);




Создай данные для таблицы dml_users:

id
name
email
age

Вставь одну строку со своими значениями.

INSERT into dml_users ()
(id, name, email, age)
VALUES
(1, 'Алексей', 'alexey@mail.ru', 25);


~~~
