~~~

Найти клиентов, у которых сумма completed-заказов выше средней суммы completed-заказов среди клиентов их города.

SELECT
e.id,
e.name,
COALESCE(sum(case when ee.status= 'completed' then ee.amount end),0) as r1
 
from customers e
LEFT JOIN orders ee
on ee.customer_id = e.id 

GROUP BY e.id, e.name

HAVING COALESCE(sum(case when ee.status= 'completed' then ee.amount end),0) > (
    SELECT
    avg(r)
    from (
        SELECT
        COALESCE(sum(case when ee1.status = 'completed' then ee1.amount end), 0) as r

        from customers e1
        LEFT JOIN orders ee1
        on ee1.customer_id = e1.id 

        where e1.city = e.city

        GROUP BY e1.customer_id

    ) t
);



Для каждого города найти клиентов, которые одновременно:

старше среднего возраста своего города;
имеют хотя бы один completed-заказ;
не имеют ни одного cancelled-заказа;
имеют сумму completed-заказов больше 15 000;
имеют количество completed-заказов выше среднего количества completed-заказов среди клиентов их города.

Вывести:

city
name
age
количество completed
сумму completed.



SELECT
e.city,
e.id,
e.name,
e.age,
count(case when ee.status = 'completed' then 1 end) as r1,
COALESCE(sum(case when ee.status = 'completed' then ee.amount end),0) as r2

from customers e
LEFT JOIN orders ee
on ee.customer_id = e.id 
where e.age > (
    SELECT
    avg(e1.age)
    from customers e1

    where e1.city = e.city
)
GROUP BY e.city, e.id, e.name, e.age

HAVING count(case when ee.status = 'completed' then 1 end) >=1 and 
count(case when ee.status = 'cancelled' then 1 end) = 0 and COALESCE(sum(case when ee.status = 'completed' then ee.amount end),0) > 15000 and count(case when ee.status = 'completed' then 1 end) > (
    SELECT
    avg(r)
    from (
        SELECT
        count(case when ee1.status = 'completed' then 1 end) as r

        from customers e2
        LEFT JOIN orders ee1
        on ee1.customer_id = e2.id 

        where e2.city = e.city

        GROUP BY ee1.customer_id
    ) t

    
);




Найти города, в которых количество клиентов с completed-заказами больше количества клиентов без completed-заказов.

select

city

from customers e
GROUP BY city

HAVING count(DISTINCT case when EXISTS (
    select 1 
    from orders
    where orders.customer_id = e.id
    and status = 'completed'
) then e.id end
) > count(DISTINCT case when not EXISTS (
    select 1
    from orders
    where orders.customer_id = e.id
    and status = 'completed'
    ) then e.id end
    );



Найти клиентов, чей возраст выше среднего возраста их города и у которых есть хотя бы один completed-заказ на сумму выше 5000.

select

name

from customers e

where age > (
    SELECT
    avg(age) as r
    from customers e1
    where e1.city = e.city
) and EXISTS (
    select 1
    from customers e2
    LEFT JOIN orders
    on orders.customer_id = e2.id
    and status = 'completed'

    where e2.id = e.id
    GROUP BY e2.id
    HAVING COALESCE(sum(case when status = 'completed' then amount end),0) > 5000
    );



Найти клиентов, у которых сумма completed-заказов выше средней суммы completed-заказов клиентов их города.


select
id,
name

from customers e4
LEFT JOIN orders 
on orders.customer_id = e4.id

GROUP BY id, name

HAVING COALESCE(sum(case when status= 'completed' then orders.amount end),0) > (
    SELECT
    avg(r)
    from (
        SELECT
        e3.id,
        sum(e2.amount) as r
        from customers e3
        LEFT JOIN orders e2
        on e2.customer_id = e3.id
        and status = 'completed'

        where e3.city = e4.city
        GROUP BY e3.id
        ) r1
);



Найти города, в которых есть хотя бы один клиент старше 40 лет и хотя бы один клиент с completed-заказом.

select

city

from customers e
where EXISTS (
    select 1
    from customers e1
    where e1.city = e.city
    and e1.age > 40
) and EXISTS (
    select 1 
    from customers e2
    LEFT JOIN orders e3
    on e3.customer_id = e2.id
    and status = 'completed'

    where e2.city = e.city
);


Найти клиентов, у которых сумма completed-заказов выше средней суммы completed-заказов клиентов их города, но сумма cancelled-заказов не превышает 5000.

select
id,
name

from customers e
LEFT JOIN orders ee
on ee.customer_id = e.id

GROUP BY id, name

HAVING COALESCE(sum(case when ee.status = 'completed' then ee.amount end),0) > (
    SELECT
    avg(r)
    from (
        SELECT
        
        sum(ee1.amount) as r
        from orders ee1
        LEFT JOIN customers e1
        on e1.id = ee1.customer_id
        where status = 'completed'

        and e1.city = e.city
        GROUP by ee1.customer_id    
    )
)    

and COALESCE(sum(case when ee.status = 'cancelled' then ee.amount end),0) <= 5000;



Найти города, в которых средний возраст клиентов выше 30, есть completed-заказы и нет клиентов, у которых сумма cancelled-заказов больше 5000.


select
e.city

from customers e
LEFT JOIN orders ee
on ee.customer_id = e.id

GROUP BY e.city

HAVING (
    select
    avg(e1.age)
    from customers e1
    where e1.city = e.city
) > 30 and EXISTS (
    select 1
    from customers e1
    LEFT JOIN orders ee1
    on ee1.customer_id = e1.id
    and ee1.status = 'completed'

    where e1.city = e.city
)

and not EXISTS (
    select 1
    from customers e2
    LEFT JOIN orders ee2
    on ee2.customer_id = e2.id
    where e2.city = e.city

    GROUP BY e2.customer_id
    
    HAVING COALESCE(sum(case when ee2.status = 'cancelled' then ee2.amount end),0) > 5000
);



Найти клиентов, у которых есть completed-заказы, сумма completed-заказов больше 15000, а количество completed-заказов больше среднего количества completed-заказов среди клиентов.


select
ee1.id,
ee1.name

from customers ee1
LEFT JOIN orders e
on e.customer_id = ee1.id

GROUP BY ee1.id, ee1.name

HAVING count(case when e.status = 'completed' then 1 end) >= 1 and COALESCE(sum(case when e.status = 'completed' then e.amount end),0) > 15000 and count(case when e.status = 'completed' then 1 end) > (
    SELECT
    avg(r)
    from (
        SELECT
        count(e1.amount) as r
        from orders e1
        where e1.status = 'completed'
        GROUP BY e1.customer_id

    ) tt
);


Найти города, в которых есть клиент старше 40 лет с completed-заказами, и при этом в этом городе нет ни одного клиента с cancelled-заказом.

select

e.city

from customers e
LEFT JOIN orders ee1
on ee1.customer_id = e.id
where e.age > 40
GROUP BY e.city

HAVING count(case when ee1.status = 'completed' then 1 end) >= 1 and not EXISTS (
SELECT 1

from customers e1
LEFT JOIN orders ee2
on ee2.customer_id = e1.id

where e1.city = e.city
GROUP BY ee2.customer_id
HAVING count(case when ee2.status = 'cancelled' then 1 end) > 0
);



Для каждого города найти клиентов, которые одновременно:

имеют хотя бы один completed-заказ;
не имеют cancelled-заказов;
имеют возраст выше среднего возраста своего города;
сумма их completed-заказов больше 15000.

Вывести:

город;
имя клиента;
возраст;
количество completed-заказов;
сумму completed-заказов.


select

e.city,
e.name,
e.age,
count(case when status = 'completed' then 1 end) as r,
COALESCE(sum(case when status = 'completed' then amount end),0) as r1

from customers e
LEFT JOIN orders
on orders.customer_id = e.id 

where age > (
    SELECT
    avg(age)
    from customers e1
    where e1.city = e.city
) 

GROUP BY e.city, e.id, e.name, e.age

HAVING count(case when status = 'completed' then 1 end) >= 1 and count(case when status = 'cancelled' then 1 end) = 0 and COALESCE(sum(case when status = 'completed' then amount end),0) > 15000;


Найти города, в которых нет ни одного клиента с cancelled-заказом.

SELECT

city

from customers e
LEFT JOIN orders ee
on ee.customer_id = e.id 

GROUP BY city

HAVING not EXISTS (
    select 1
    from customers e1
    LEFT JOIN orders ee1
    on ee1.customer_id = e1.id

    where e1.city = e.city

    GROUP BY ee1.customer_id
    HAVING COUNT(
        CASE WHEN ee1.status = 'cancelled' THEN 1 END
    ) > 0
);


Найти клиентов, чей возраст выше среднего возраста клиентов из их города.

SELECT

name

from customers e
where age > (
    select
    avg(age)
    from customers e1
    where e1.city = e.city
);



Найти клиентов, у которых сумма completed-заказов выше средней суммы completed-заказов среди всех клиентов.


SELECT
name

from customers
LEFT JOIN orders 
on orders.customer_id = customers.id
GROUP BY name
HAVING COALESCE(sum(case when status='completed' then amount end),0) > (
    SELECT
    avg(r)
    from (
        select
        customer_id,
        sum(amount) as r
        from orders 
        where status = 'completed'
        GROUP BY customer_id
    ) r1
);



Найти клиентов, у которых количество completed-заказов больше среднего количества completed-заказов на клиента.

select
name

from customers
LEFT JOIN orders
on orders.customer_id = customers.id
GROUP BY name 
HAVING count(case when status = 'completed' then 1 end) > (
    SELECT
    avg(r)
    from (
        select
        customer_id,
        count(case when status = 'completed' then 1 end) as r
        from orders
        GROUP by customer_id
    ) r1
);


Найти клиентов, у которых сумма всех заказов больше 50000 и выше средней суммы всех заказов среди клиентов.

select
name

from customers
LEFT JOIN orders
on orders.customer_id = customers.id
GROUP BY name 

HAVING COALESCE(sum(amount),0) > 50000 and COALESCE(sum(amount),0)  > (
    SELECT
    avg(r)
    from (
        SELECT
        customer_id,
        sum(amount) as r
        from orders
        GROUP BY customer_id
    ) r1
);


Найти клиентов, чей возраст выше среднего возраста клиентов их города.

select
name

from customers e
where e.age > (
    SELECT
    avg(e1.age)
    from customers e1
    where e1.city = e.city
    
);


Найти клиентов, у которых есть заказ с суммой выше их собственной средней суммы заказа.

select
name

from customers
LEFT JOIN orders 
on orders.customer_id = customers.id
 where amount > (
    select
    avg(r)
    from (
        SELECT
        sum(amount) as r
        from orders
        GROUP BY customer_id
    ) r1
);



Найти клиентов, которые живут в городах, где средний возраст клиентов выше 30, и при этом имеют хотя бы один completed-заказ.

select
name,
city
from customers e
where (
    SELECT
    avg(age)
    from customers e1
    where e1.city = e.city
) > 30 and EXISTS (
    select 1
    from orders
    where orders.customer_id = e.id
    and status = 'completed'
);



Найти города, в которых есть клиент старше 40 лет, но нет ни одного completed-заказа.

select

city

from customers e
where age > 40 and not EXISTS (
    select 1
    from orders
    where orders.customer_id = e.id
    and status = 'completed'
);


Найти клиентов, у которых количество completed-заказов больше количества completed-заказов любого другого клиента.

select

name

from customers
LEFT JOIN orders
on orders.customer_id = customers.id
GROUP BY name
HAVING count(case when status= 'completed' then 1 end) > all (
    SELECT
    count(case when status= 'completed' then 1 end) as r
    from orders e1
    where e1.customer_id <> customers.id
    GROUP by customer_id
);



Задача №2 — Subquery

Теперь немного сложнее.

Нужно найти сотрудников, которые получают зарплату выше средней зарплаты именно в их городе.

select 
*
from employees t 
where salary > (
    select
    avg(salary)
    from employees
    where city = t.city
);







Задача №3

Найди сотрудников, которые получают максимальную зарплату в своём городе.


select 
*
from employees t 
where salary = (
    select 
    max(salary)
    from employees
    where city = t.city
);





~~~~
