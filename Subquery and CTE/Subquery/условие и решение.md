~~~
коррелированный подзапрос:


Найти клиентов, чей возраст выше среднего возраста клиентов из их города.

SELECT

name

from customers e
where age > (
    select
    avg(age)
    from customers e1
    where e1.city = e.city
);



Найти клиентов, у которых сумма completed-заказов выше средней суммы completed-заказов среди всех клиентов.


SELECT
name

from customers
LEFT JOIN orders 
on orders.customer_id = customers.id
GROUP BY name
HAVING COALESCE(sum(case when status='completed' then amount end),0) > (
    SELECT
    avg(r)
    from (
        select
        customer_id,
        sum(amount) as r
        from orders 
        where status = 'completed'
        GROUP BY customer_id
    ) r1
);



Найти клиентов, у которых количество completed-заказов больше среднего количества completed-заказов на клиента.

select
name

from customers
LEFT JOIN orders
on orders.customer_id = customers.id
GROUP BY name 
HAVING count(case when status = 'completed' then 1 end) > (
    SELECT
    avg(r)
    from (
        select
        customer_id,
        count(case when status = 'completed' then 1 end) as r
        from orders
        GROUP by customer_id
    ) r1
);


Найти клиентов, у которых сумма всех заказов больше 50000 и выше средней суммы всех заказов среди клиентов.

select
name

from customers
LEFT JOIN orders
on orders.customer_id = customers.id
GROUP BY name 

HAVING COALESCE(sum(amount),0) > 50000 and COALESCE(sum(amount),0)  > (
    SELECT
    avg(r)
    from (
        SELECT
        customer_id,
        sum(amount) as r
        from orders
        GROUP BY customer_id
    ) r1
);


Найти клиентов, чей возраст выше среднего возраста клиентов их города.

select
name

from customers e
where e.age > (
    SELECT
    avg(e1.age)
    from customers e1
    where e1.city = e.city
    
);


Найти клиентов, у которых есть заказ с суммой выше их собственной средней суммы заказа.

select
name

from customers
LEFT JOIN orders 
on orders.customer_id = customers.id
 where amount > (
    select
    avg(r)
    from (
        SELECT
        sum(amount) as r
        from orders
        GROUP BY customer_id
    ) r1
);



Найти клиентов, которые живут в городах, где средний возраст клиентов выше 30, и при этом имеют хотя бы один completed-заказ.

select
name,
city
from customers e
where (
    SELECT
    avg(age)
    from customers e1
    where e1.city = e.city
) > 30 and EXISTS (
    select 1
    from orders
    where orders.customer_id = e.id
    and status = 'completed'
);



Найти города, в которых есть клиент старше 40 лет, но нет ни одного completed-заказа.

select

city

from customers e
where age > 40 and not EXISTS (
    select 1
    from orders
    where orders.customer_id = e.id
    and status = 'completed'
);


Найти клиентов, у которых количество completed-заказов больше количества completed-заказов любого другого клиента.

select

name

from customers
LEFT JOIN orders
on orders.customer_id = customers.id
GROUP BY name
HAVING count(case when status= 'completed' then 1 end) > all (
    SELECT
    count(case when status= 'completed' then 1 end) as r
    from orders e1
    where e1.customer_id <> customers.id
    GROUP by customer_id
);



Задача №2 — Subquery

Теперь немного сложнее.

Нужно найти сотрудников, которые получают зарплату выше средней зарплаты именно в их городе.

select 
*
from employees t 
where salary > (
    select
    avg(salary)
    from employees
    where city = t.city
);







Задача №3

Найди сотрудников, которые получают максимальную зарплату в своём городе.


select 
*
from employees t 
where salary = (
    select 
    max(salary)
    from employees
    where city = t.city
);





~~~~
