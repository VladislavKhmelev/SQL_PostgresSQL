~~~
Найти топ-10 клиентов по сумме completed-заказов, но учитывать только клиентов:

со статусом active;
у которых есть хотя бы один заказ дороже 50000;
с минимум 2 completed-заказами.

Вывести:

customer_id
name
order_count
total_amount
rank



EXPLAIN ANALYZE
with tt as (
select
*,

COALESCE(sum(amount) over(PARTITION BY customer_id),0) as r,
COUNT(orders_opt.id) over(PARTITION BY customer_id) as r1,

count(case when amount > 50000 then 1 end) over(PARTITION BY customer_id) as r2


from customers_opt
LEFT JOIN orders_opt
on orders_opt.customer_id = customers_opt.id
and orders_opt.status = 'completed'

where customers_opt.status = 'active'


),

tt1 as (

    select
    *,
    rank() over(ORDER BY r DESC) as r3

    from tt
)

select
customer_id,
name,
r,
r1,
r2

from tt1
where r2 >= 1 and r3 <= 10 and r1 >= 2;






Найти топ-10 клиентов по сумме completed-заказов, но учитывать только клиентов:

со статусом active;
у которых есть хотя бы один заказ дороже 50000;
с минимум 2 completed-заказами.

Вывести:

customer_id
name
order_count
total_amount
rank

CREATE index idx
on customers_opt(status)
where status = 'active';

CREATE index idx1
on orders_opt(status)
where status = 'completed';


EXPLAIN ANALYZE
select
customers_opt.id,
name,

COALESCE(sum(amount),0) as r,
count(orders_opt.id) as r1

from customers_opt
LEFT JOIN orders_opt
on orders_opt.customer_id = customers_opt.id
and orders_opt.status = 'completed'

where customers_opt.status = 'active'

GROUP BY customers_opt.id, name 

HAVING count(orders_opt.id) >= 2 and count(case when amount > 50000 then 1 end )>= 1

ORDER BY r desc

limit 10;





Найти клиентов, у которых:

completed-заказов минимум 3;
сумма completed-заказов больше 200000.

Использовать:

GROUP BY
HAVING
JOIN

Создать подходящий partial index.

Проверить эффективность через EXPLAIN ANALYZE.

CREATE index idx
on orders_opt(status)
where status = 'completed';

EXPLAIN ANALYZE
select
customer_id,

count(orders_opt.id) as r,
sum(amount) as r1

from orders_opt
where status = 'completed'

GROUP BY customer_id

HAVING count(orders_opt.id) >= 3 and sum(amount) > 200000;






Для каждого клиента определить его последний заказ.

Вывести:

customer_id
order_id
amount
created_at

Использовать оконную функцию.

Затем:

выполнить EXPLAIN ANALYZE;
определить узкое место;
подобрать индекс;
повторить EXPLAIN ANALYZE.


CREATE index idx
on orders_opt(customer_id, created_at);


EXPLAIN ANALYZE
with tt as (
select
*,
ROW_NUMBER() OVER(PARTITION BY customer_id ORDER BY orders_opt.created_at DESC) as r

from orders_opt



)

select
customer_id,
id,
amount,
created_at

from tt
where r = 1;





Найти клиентов:

из Ufa;
со статусом active;
имеющих completed-заказы;
сумма заказов больше 100000.

Вывести:

id
name
city
order_count
total_amount

Использовать:

CTE
JOIN
GROUP BY
индексы
EXPLAIN ANALYZE

Оптимизировать запрос.


CREATE INDEX idx
on customers_opt(city, status)
where status = 'active';

CREATE index idx1
on orders_opt(status, amount)
where status = 'completed';


EXPLAIN ANALYZE
select
customers_opt.id,
name,
city,

count(orders_opt.id) as r1,
COALESCE(sum(amount),0) as r

from customers_opt
LEFT JOIN orders_opt
on orders_opt.customer_id = customers_opt.id
and orders_opt.status = 'completed'


where city = 'Ufa' and customers_opt.status = 'active'

GROUP BY customers_opt.id, name, city

HAVING COALESCE(sum(amount),0) > 100000;




Дан запрос:

найти клиентов,
у которых email начинается с 'admin'

Сначала написать обычный запрос.

Затем выполнить EXPLAIN ANALYZE.

Оптимизировать запрос так, чтобы PostgreSQL мог эффективно использовать индекс.


CREATE index idx
on customers_opt(email)
where email like 'admin%';

EXPLAIN ANALYZE
select
id,
name


from customers_opt
where email like 'admin%';






Решить задачу:

найти клиентов,
у которых есть completed-заказы.

Использовать EXISTS.

Проанализировать план выполнения и определить, какие индексы будут полезны.


CREATE INDEX idx
on orders_opt(status)
where status = 'completed';

EXPLAIN ANALYZE
select
customers_opt.id,
name

from customers_opt
LEFT JOIN orders_opt
on orders_opt.customer_id = customers_opt.id
and orders_opt.status = 'completed';






Найти клиентов, у которых есть хотя бы один заказ:

amount > 100000

Использовать подзапрос.

Проанализировать EXPLAIN ANALYZE.

После этого оптимизировать запрос.

CREATE index idx
on orders_opt(amount);




EXPLAIN ANALYZE
select
customers_opt.id,
name,
count(orders_opt.id) as r


from customers_opt
LEFT JOIN orders_opt
on orders_opt.customer_id = customers_opt.id
and amount > 100000

GROUP BY id, name 

HAVING count(orders_opt.id) >= 1;




Найти:

20 самых дорогих completed-заказов
за последние 6 месяцев.

Проанализировать EXPLAIN ANALYZE.

Создать оптимальный индекс.


CREATE index idx
on orders_opt(status, amount, created_at)
where status = 'completed';

EXPLAIN ANALYZE
select
*

from orders_opt
where status = 'completed' and created_at >= CURRENT_DATE - INTERVAL '6 month'

ORDER BY amount DESC

limit 20;





Получить имена клиентов и их заказы, где:

customer status = active
order status = completed
amount > 30000

Оптимизируй запрос с помощью индексов.


CREATE INDEX idx
on customers_opt(status)
where status = 'active';

CREATE index idx1
on orders_opt(status, amount)
where status = 'completed';


EXPLAIN ANALYZE
select
customers_opt.id,
name,
orders_opt.id,
created_at

from customers_opt
INNER JOIN orders_opt
on orders_opt.customer_id = customers_opt.id
where customers_opt.status = 'active' and orders_opt.status = 'completed'
and amount > 30000;





Получить:

customer_id
количество заказов
сумму completed-заказов

для всех клиентов.

Используй:

JOIN
GROUP BY

Проанализируй запрос через EXPLAIN ANALYZE и оптимизируй его.



CREATE index idx
on orders_opt(customer_id);



EXPLAIN ANALYZE
select
customer_id,
count(orders_opt.id) as r,
COALESCE(sum(case when status = 'completed' then amount end),0) as r1


from customers_opt
LEFT JOIN orders_opt
on orders_opt.customer_id = customers_opt.id

GROUP BY customer_id;

То есть правило такое:

Если запросу нужны все строки — универсальный индекс. Если запросу нужна только определённая подгруппа строк — partial index может быть выгоднее.








Найти узкое место

Дан запрос:

найти completed-заказы
с amount > 50000
за последние 2 года.

Выполни EXPLAIN ANALYZE.

Оптимизируй запрос с помощью индекса.

Повтори EXPLAIN ANALYZE и сравни результаты.



EXPLAIN ANALYZE
select
*
from orders_opt
where status = 'completed'
and amount > 50000
and created_at >= CURRENT_DATE - INTERVAL '2 years';


CREATE index idx
on orders_opt(status, amount, created_at)
where status = 'completed';






Создай индекс, который будет полезен для поиска:

активных клиентов
из Ufa

То есть запрос содержит:

city = 'Ufa'
AND status = 'active'

Используй подходящий тип индекса и проверь план.



CREATE INDEX idx
on customers_opt(city, status)
where city = 'Ufa' and status = 'active';





Создай частичный индекс только для:

orders_opt.status = 'completed'

После этого найди completed-заказы с:

amount > 50000

Проверь EXPLAIN ANALYZE.



CREATE index idx3
on orders_opt(status)
where status = 'completed';

EXPLAIN ANALYZE
SELECT
*
from orders_opt

WHERE status = 'completed' and amount > 50000;



Expression Index

Создай индекс для запроса:

WHERE LOWER(email) = 'test@example.com'

Проверь EXPLAIN ANALYZE.



CREATE index idx3
on customers_opt(LOWER(email));

EXPLAIN ANALYZE
SELECT
*
from customers_opt

WHERE LOWER(email) = 'test@example.com';




3.2 — Hash

Создай Hash индекс на customers_opt.email.

Проверь точный поиск:

WHERE email = 'test@example.com'

Используй EXPLAIN ANALYZE.



CREATE index idx
on customers_opt USING Hash (email);

EXPLAIN ANALYZE
SELECT
*
from customers_opt

where email = 'user13@example.com';






Создай составной индекс для запроса:

customer_id = 20
AND status = 'completed'
ORDER BY created_at DESC

Проверь через EXPLAIN ANALYZE.



EXPLAIN ANALYZE
SELECT
*
from orders_opt
where status = 'completed' and customer_id = 20

ORDER BY created_at DESC;


CREATE index idx2
on orders_opt(status, customer_id, created_at);



Получить 10 самых дорогих completed-заказов.

Использовать:

WHERE
ORDER BY
LIMIT

Подбери индекс и проверь план.


EXPLAIN ANALYZE
SELECT
*
from orders_opt
where status = 'completed'

ORDER BY amount DESC

limit 10;


CREATE index idx2
on orders_opt(status, amount);




Найди:

completed-заказы
клиента с customer_id = 10
отсортированные от новых к старым

Создай подходящий индекс.

Проверь через EXPLAIN ANALYZE.

EXPLAIN ANALYZE
SELECT
*
from orders_opt
where status = 'completed' and customer_id = 10

ORDER BY created_at DESC;

CREATE INDEX idx1
on orders_opt(customer_id, status, created_at);





Создай составной индекс:

status + created_at

для orders_opt.

Найди:

completed-заказы
после 2025-01-01

Проверь EXPLAIN ANALYZE.


CREATE INDEX idx2
on orders_opt(status, created_at);




EXPLAIN ANALYZE
SELECT
*
from orders_opt
where created_at > date '2025-01-01' and status = 'completed';




Создай индекс на customers_opt.age.

Проверь запрос поиска клиентов старше 40 лет через EXPLAIN ANALYZE.

Затем удали созданный индекс и снова выполни EXPLAIN ANALYZE.



CREATE INDEX idx1
on customers_opt(age);


EXPLAIN ANALYZE
select
*
from customers_opt
where age> 40;

drop INDEX if EXISTS idx1;




Найди 20 последних клиентов по created_at.

Используй:

ORDER BY
LIMIT
EXPLAIN ANALYZE

Создай индекс, который считаешь подходящим.


CREATE INDEX idx1
on customers_opt(created_at);



EXPLAIN ANALYZE
SELECT
*
from customers_opt

ORDER BY created_at DESC

limit 20;







Создай индекс на orders_opt.amount.

Найди заказы, где:

amount > 50000

Используй EXPLAIN ANALYZE.

CREATE INDEX idx 
on orders_opt(amount);



EXPLAIN ANALYZE
SELECT
*
from orders_opt
where amount > 50000;


 Seq Scan on orders_opt  (cost=0.00..1.25 rows=7 width=60) (actual time=0.019..0.021 rows=3.00 loops=1)
   Filter: (amount > '50000'::numeric)
   Rows Removed by Filter: 17
   Buffers: shared hit=1
 Planning:
   Buffers: shared hit=18 read=3
 Planning Time: 0.557 ms
 Execution Time: 0.038 ms
(8 строк)


Создай индекс на orders_opt.created_at.

Найди все заказы после '2025-01-01'.

Проверь выполнение через EXPLAIN ANALYZE.

CREATE INDEX idx3 
on orders_opt(created_at);



EXPLAIN ANALYZE
SELECT
*
from orders_opt
where created_at > date '2025-01-01';

hh=# EXPLAIN ANALYZE
hh-# SELECT
hh-# *
hh-# from orders_opt
hh-# where created_at > date '2025-01-01';
                                               QUERY PLAN
---------------------------------------------------------------------------------------------------------
 Seq Scan on orders_opt  (cost=0.00..1.25 rows=7 width=60) (actual time=0.036..0.040 rows=20.00 loops=1)
   Filter: (created_at > '2025-01-01'::date)
   Buffers: shared hit=1
 Planning Time: 0.142 ms
 Execution Time: 0.066 ms
(5 строк)






Создай индекс на customers_opt.email.

Проверь через EXPLAIN ANALYZE поиск клиента по конкретному email.

CREATE INDEX idx1
on customers_opt(email);



EXPLAIN ANALYZE
select
*
from customers_opt
where email = 'user13@example.com';




Создай индекс на customers_opt.city.

Затем выполни запрос, который находит всех клиентов из города 'Ufa'.

CREATE INDEX idx
on customers_opt(city);

select *
from customers_opt
where city = 'Ufa';





CREATE INDEX idx_orders_user_id
ON orders(user_id);


EXPLAIN ANALYZE
SELECT *
FROM users u
JOIN orders o
    ON u.id = o.user_id
WHERE u.id = 99999;



Без индекса


Сначала удаляем индекс:

DROP INDEX IF EXISTS idx_users_email;

Потом выполняем:

EXPLAIN ANALYZE
SELECT *
FROM users
WHERE email = 'user99999@mail.ru';



 Seq Scan on users  (cost=0.00..2074.00 rows=1 width=31) (actual time=27.903..27.905 rows=1.00 loops=1)
   Filter: (email = 'user99999@mail.ru'::text)
   Rows Removed by Filter: 99999
   Buffers: shared read=824
 Planning:
   Buffers: shared hit=18 read=3 dirtied=3
 Planning Time: 2.521 ms
 Execution Time: 27.939 ms
(8 строк)



Создаём индекс
CREATE INDEX idx_users_email
ON users(email);

И снова тот же самый запрос:

EXPLAIN ANALYZE
SELECT *
FROM users
WHERE email = 'user99999@mail.ru';


 Index Scan using idx_users_email on users  (cost=0.42..8.44 rows=1 width=31) (actual time=0.120..0.121 rows=1.00 loops=1)
   Index Cond: (email = 'user99999@mail.ru'::text)
   Index Searches: 1
   Buffers: shared hit=1 read=3
 Planning:
   Buffers: shared hit=15 read=1
 Planning Time: 2.098 ms
 Execution Time: 0.149 ms
(8 строк)



Если хочешь обновить статистику

После существенных изменений данных можно отдельно выполнить:

ANALYZE users;

А затем снова:

EXPLAIN ANALYZE
SELECT *
FROM users
WHERE email = 'user99999@mail.ru';
Важное различие
ANALYZE users;

→ обновляет статистику таблицы.

EXPLAIN ANALYZE SELECT ...;

→ показывает план и реально выполняет запрос.

~~~
