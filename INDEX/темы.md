~~~


B-tree — основной универсальный индекс.
Hash — точное сравнение =.
GIN — JSONB, массивы, полнотекстовый поиск.
GiST — диапазоны, геометрия и сложные отношения.
BRIN — огромные таблицы и диапазоны блоков.
Обычный, уникальный, частичный и составной индекс.
Порядок столбцов в составном индексе.
Левый префикс.
Expression Index — индекс по выражению, например LOWER(email).
INCLUDE и связь с Index Only Scan.
Индексы и NULL.
Селективность.
Почему PostgreSQL иногда выбирает Seq Scan вместо Index Scan.
Разницу между Index Scan / Index Only Scan / Bitmap Index Scan / Bitmap Heap Scan.
Почему индекс ускоряет чтение, но увеличивает стоимость INSERT / UPDATE / DELETE.
Как проверять всё это через EXPLAIN и EXPLAIN ANALYZE.
И отдельно разобрали узлы:
Nested Loop
Hash Join
Merge Join
Materialize
Memoize
Gather
Gather Merge
Sort
~~~
